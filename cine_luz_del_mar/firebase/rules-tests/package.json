{
  "name": "cine-luz-del-mar-rules-tests",
  "private": true,
  "type": "module",
  "description": "Tests de las reglas de seguridad de Firestore contra la Emulator Suite",
  "scripts": {
    "test": "node --test",
    "test:emulator": "firebase emulators:exec --only firestore --project demo-cine-luz-del-mar \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
