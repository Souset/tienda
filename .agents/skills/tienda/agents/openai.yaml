interface:
  display_name: "Tienda"
  short_description: "Repo-specific patterns and workflows for tienda"
  default_prompt: "Use the tienda repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true